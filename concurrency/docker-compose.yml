version: "3.1"

services:
  db:
    image: postgres:13.1
    privileged: true
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: optikrus
      POSTGRES_PASSWORD: password123456
      POSTGRES_DB: pdb
